<!DOCTYPE html>
<html>
    <head>
        <title>List of users for <%=shopData.shopName%></title>
        <link rel="stylesheet"  type="text/css" href="/main.css" />
    </head>
    <body>
        <h1>List of users for <%=shopData.shopName%></h1>
        <table>
            <tr>
                <th>Username</th>
                <th>Name</th>
            </tr>
            <% for (let {username, first_name, last_name} of userData) { %>
            <tr>
                <td><%= username %> </td>
                <td><%= first_name %> <%= last_name %></td>
            </tr>
            <% } %>
        </table>
    </body>
</html>